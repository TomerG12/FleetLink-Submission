package io.github.tomerg12.fleetlink.shared.protocol;

/**
 * Identifies the registered player's outcome in one persistent history entry.
 */
public enum MatchOutcome {
    /** The registered player won the completed match. */
    WIN,

    /** The registered player lost the completed match. */
    LOSS
}
